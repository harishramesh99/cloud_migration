<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Patient</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <%- include('./../partials/navbar') %>
    <div class="container">
      <h1>Edit Patient</h1>
      <form action="/api/patients/edit/<%= patient.id %>" method="POST">
        <div class="form-group">
          <label for="name">Name</label>
          <input
            type="text"
            id="name"
            name="name"
            value="<%= patient.data().name %>"
            required
          />
        </div>
        <div class="form-group">
          <label for="age">Age</label>
          <input
            type="number"
            id="age"
            name="age"
            value="<%= patient.data().age %>"
            required
          />
        </div>
        <div class="form-group">
          <label for="contact">Contact</label>
          <input
            type="email"
            id="contact"
            name="contact"
            value="<%= patient.data().contact %>"
            required
          />
        </div>
        <div class="form-group">
          <label for="medicalHistory">Medical History</label>
          <textarea id="medicalHistory" name="medicalHistory" required>
<%= patient.data().medicalHistory %></textarea
          >
        </div>
        <button type="submit" class="btn btn-save">Save</button>
      </form>
    </div>
  </body>
</html>
