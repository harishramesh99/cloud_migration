<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Appointment</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <%- include('./../partials/navbar') %>
    <div class="container">
      <h1>Edit Appointment</h1>
      <form
        action="/api/appointments/edit/<%= appointment.id %>?_method=PUT"
        method="POST"
      >
        <div class="form-group">
          <label for="patient">Patient</label>
          <input
            type="text"
            id="patient"
            name="patient"
            value="<%= appointment.data().patient %>"
            required
          />
        </div>
        <div class="form-group">
          <label for="doctor">Doctor</label>
          <input
            type="text"
            id="doctor"
            name="doctor"
            value="<%= appointment.data().doctor %>"
            required
          />
        </div>
        <div class="form-group">
          <label for="date">Date</label>
          <input
            type="datetime-local"
            id="date"
            name="date"
            value="<%= appointment.data().date %>"
            required
          />
        </div>
        <button type="submit" class="btn btn-save">Save</button>
      </form>
    </div>
  </body>
</html>
